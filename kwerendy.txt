SELECT id,imie,nazwisko FROM `personel` WHERE status = "policjant";

SELECT typ,count(id) as LiczbaPojazdow FROM `pojazdy` GROUP BY typ;

SELECT personel.id,personel.nazwisko FROM `personel`,rejestr WHERE personel.id != rejestr.id_personel;

insert into rejestr (data,id_personel,id_pojazd) VALUES (CURRENT_DATE,1,14);
